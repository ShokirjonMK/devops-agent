# --- AI ---
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_API_KEY=
AI_PROVIDER=openai

# --- Auth / shifrlash (v2) ---
# openssl rand -base64 32  (32 bayt = 256 bit)
JWT_SECRET=
ENCRYPTION_MASTER_KEY_B64=

# Telegram Login Widget + bot (bot uchun shart)
TELEGRAM_BOT_TOKEN=

# --- SSH ---
SSH_KEYS_DIR=./ssh-keys
SSH_PRIVATE_KEY_B64=
SSH_CONNECT_RETRIES=3
SSH_RETRY_BACKOFF_SECONDS=2

# --- Telegram bot (polling) ---
HTTP_RETRIES=3
POLL_INTERVAL_SEC=2
POLL_MAX_ROUNDS=120
