# Dev: `docker compose` avtomatik qo‘shadi. Productionda o‘chirib qo‘yish mumkin.
services:
  api:
    volumes:
      - ./backend/app:/app/app:ro
    environment:
      UVICORN_RELOAD: "1"
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
